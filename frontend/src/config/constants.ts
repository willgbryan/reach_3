// embedding settings - api/file-embed
export const EMBEDDING_TABLE_NAME = 'document_sections'
// Limit for the number of documents in each batch insert to reduce load.
export const BATCH_UPSERT_LIMIT = 10
